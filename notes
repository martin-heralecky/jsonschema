toto uz neplati, protoze by se to blbe pouzivalo v phpku. radsi at v PHP je typ co se snadno pouziva v phpku a pres
#[Type()] nebo #[Schema()] se rekne jak to ma vypadat v jsonu
// todo a  /** @var int bla. */ public int $bravo = 123;  se teda treatuje jako zkratka pro:
///**
// * bla.
// */
//#[DefaultVal(123)]
//class bravo extends Integer
//{
//}

create dir Introspector and move contents of TypeParser namespace into it
defaulty a examply nad DateTime co se castuje do stringu

- kdyz bude problem, muzu udelat validacni attributy implementovat nejaky iface a rict ze union muze na sobe mit validace jakykoliv a plati jen ty co se daji uplatnit podle typu v runtimu

RtrimSlashCast castuje string na string
